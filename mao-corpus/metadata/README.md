# Metadata

`source-manifest.json` is generated by `inspect_pdfs.py` and records immutable file identity and extraction state. `mapping.schema.json` describes the human-confirmed mapping file required before formal attribution.

Do not infer a book title from a filename. Keep uncertain candidates as `unmapped` or `disputed`.
